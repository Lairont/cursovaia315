@model List<cursovaia2.ModelsDb.DeliveryMethodDb>
@{ ViewData["Title"] = "Способы доставки"; }
<div class="admin-panel">
    <form method="post" action="/Admin/SaveDeliveryMethod" class="admin-form">
        @Html.AntiForgeryToken()
        <div><label>Название</label><input type="text" name="name" required></div>
        <div><label>Цена</label><input type="number" step="0.01" name="price" value="0"></div>
        <button type="submit" class="btn btn-primary">Добавить</button>
    </form>
</div>
<div class="admin-panel">
    <table class="admin-table">
        <thead><tr><th>ID</th><th>Название</th><th>Цена</th><th></th></tr></thead>
        <tbody>@foreach (var m in Model) {
            <tr><td>@m.Id</td><td>@m.Name</td><td>@m.Price</td>
            <td><form method="post" action="/Admin/DeleteDeliveryMethod" onsubmit="return confirm('?')">@Html.AntiForgeryToken()<input type="hidden" name="id" value="@m.Id"><button class="btn-admin btn-admin--danger">×</button></form></td></tr>
        }</tbody>
    </table>
</div>
